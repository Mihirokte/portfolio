# Mihir Okte — portfolio

Personal site. React + Vite + TypeScript + Tailwind, with a three.js scene.

- `npm run dev` — local dev server
- `npm run build` — builds to `docs/`, which GitHub Pages serves at https://mihirokte.info
- Content lives in `src/content.ts`; colors in `src/palette.ts`.
